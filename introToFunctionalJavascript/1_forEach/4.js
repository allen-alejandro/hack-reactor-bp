

// Write a function called printNames that takes an array of names and console.logs them:

var printNames = function (names) {
  names.forEach(x => console.log(x)); 
};

printNames(['Tom', 'Jerry', 'Arnold', 'Casper']);