const computeTripledAreaOfARectangle = (length, width) => {
  return length * width * 3;
}


const computePerimeterOfACircle = radius => {
  return 2 * Math.PI * radius;
}

const computeAreaOfACircle = radius => {
  return Math.PI * (radius * radius);
}