


const computePower = (num, exponent) => {
  return Math.pow(num, exponent);
}


console.log(computePower(2, 3));


const computeSquareRoot = num => {
  return Math.sqrt(num);
}


const doubleSquareRootOf = num => {
  return Math.sqrt(num) * 2;
}